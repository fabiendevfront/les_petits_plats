import { stringReformat} from "./tools.js";
import { newDataFormat, displayRecipesCards } from "../app.js";
import { tagModel } from "../models/TagModel.js";

export const searchByTag = () => {
    console.log(newDataFormat);
    // console.log(tagModel().arrayTags());
    // displayRecipesCards()
};